<div>
  {{!-- CSS obviously --}}
  <image src="images/scene-{{id}}.png" style="height: 400px; display: block"/>

  <p>{{text}}</p>

  <div class="choices">
    {{#each choices}}
      <button data-scene-id="{{sceneId}}">{{description}}</button>
    {{/each}}
  </div>

  {{!-- TODO use <source> tags instead? --}}
  <audio src="audio/scene-{{id}}.m4a" preload="auto"></audio>
</div>